1. working instance api

2. user content size limits (100mb, more for supporter accounts)
3. upload / download(?) limits per user (~50GB up 150GB down per month)
4. overall rate limit per user (~5000 per hour)
5. cache control headers on everything
6. cache user uploads

figured out ips use the taefik header thing
7. per ip account creation limit (10 per year)
8. per ip hourly login limit (~5) success or fail

9. respect content privacy
10. moderation api

11. timestamp db entries created / modified

12. friend api
